# Solana RPC endpoint (use local validator for development)
RPC_URL=http://127.0.0.1:8899

# Token-2022 mint address (will be set after creating token)
# This will be a Token-2022 token with transfer tax enabled
TOKEN_MINT_ADDRESS=

# Mint authority keypair path (relative to this package)
MINT_AUTHORITY_KEYPAIR_PATH=./keypairs/mint-authority.json
